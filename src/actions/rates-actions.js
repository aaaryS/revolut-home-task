import { createAction } from 'redux-actions'
import axios from 'axios'

import rc from '../constants/rates-constants'

export const fetch = (p = {}) => (d, getState) => {
  // d(createAction(rc.FETCH_ALL)({tasks: tasks.toJS()}))
}
